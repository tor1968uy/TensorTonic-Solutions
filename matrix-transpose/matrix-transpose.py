import numpy as np

def matrix_transpose(A):
    """
    Return the transpose of matrix A (swap rows and columns).
    """
    # 1. Usando la función nativa de NumPy (más explícita)
    return np.transpose(A)